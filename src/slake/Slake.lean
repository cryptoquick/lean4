/-
Copyright (c) 2026 Hunter Beast. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Hunter Beast
-/
module

import Slake.CLI
import Slake.Config

/-!
# Slake

Root public surface for the **Systems Lean** twin of Lake.

This module is the classic-host import root for the Slake driver as it grows.
Freestanding residual-green product cores (dep graph, TOML subset, traces,
cache index, proc spawn, …) live under `Systems.*` and are not re-exported here
until the driver needs them on the classic path.

## Phase 2 MVP status (through W107) (honest residual)

* CLI: `Slake.CLI` — CLAIMED `build` / `clean` / `env` / `test`; classic thin forwards
  `script` / `exe` / `lint` / `check-build` / `check-test` / `check-lint` / `query` / `shake` / `update` / `pack` / `unpack` / `cache` / `lean` / `scripts` / `new` / `init` / `serve` / `upload` / `translate-config` / `run` / `setup-file` / `self-check` / `version-tags` / `query-kind` / `resolve-deps` / `reservoir-config` / `exec` / `upgrade` (outside CLAIMED); `--help` / `--version`
  (classic host: `build` delegates to `lake build`; `test` delegates to `lake test`; thin forwards →
  `lake …` via `IO.Process` default; optional FS_PROC/PIPE for
  `build` / `test` / `exe` / `lint` / `script` / `clean` / `update` / `pack` / `cache` / `unpack` / `query` / `shake` / `serve` / `upload` / `lean` / `scripts` / `setup-file` / `self-check` / `version-tags` / `translate-config` / `run` / `new` / `init` / `check-build` — clean is empty-rest-only dual residual; pack/cache/unpack/query/shake/serve/upload/lean/scripts/setup-file/self-check/version-tags/translate-config/run/check-build chdir(pkg) + resolveLakeAbs pre-chdir; `new`/`init` chdir(cwd) bootstrap (no package walk-up) + resolveLakeAbs pre-chdir;
  freestanding `Systems.Proc` dogfood at `tests/slake/proc_dogfood`; pipe freestanding shipped;
  classic CLI residual).
  CLAIMED parity slice is `build` / `clean` / `env` / `test` (test = lake-test exit 0; not freestanding TCB).
* Config: `Slake.Config` — host TOML subset for package identity + plan nodes on CLAIMED `env`/`build` (`SLAKE_DEPGRAPH` / `SLAKE_PLAN_ONLY` / `SLAKE_NATIVE_CHECK` / `SLAKE_NATIVE_OLEAN` / `SLAKE_NATIVE_BUILD` / `SLAKE_NATIVE_C` / `SLAKE_NATIVE_OBJ` / `SLAKE_NATIVE_IRLINK` / `SLAKE_NATIVE_AR` / `SLAKE_NATIVE_EXE` / `SLAKE_NATIVE_LINK` / `SLAKE_NATIVE_GRAPH` / `SLAKE_NATIVE_SEAL`); optional package `srcDir` + dotted module path resolution (A6) + optional per-`[[lean_lib]]` `roots` / `srcDir` (A16) + optional per-lib `globs` ones/`.+`/`.*` recursive multi-level plan expand (A24/A25 — not full Lake Glob / faceting / package imports / freestanding build TCB / CLAIMED) + optional `[[require]]` name+safe relative path (A26 path-require LEAN_PATH + path-dep olean precompile — not git/url / not Lake resolve-deps / not freestanding build TCB / not CLAIMED) + A29 sibling confining `path = "../dep"` under package-parent confining root (refuse multi-`..` escape / bare `..` / absolute — not full Lake path require / not workspace walk-up / not freestanding build TCB / not CLAIMED) + A27 path-dep → root olean cascade (`path-dep-olean-newer` on consumer imports of path-dep plan modules — not freestanding build TCB / not CLAIMED) + A28 path-dep source-hash fold into A11 frozen `deps <hex>` (not Lake package-transitive / not freestanding build TCB / not CLAIMED) + A30 package-transitive plan subset (import-driven path-dep plan modules folded into root plan when imported — not Lake resolve-deps / not every dep plan module / not freestanding build TCB / not CLAIMED) + A31 freestanding-adjacent path-dep plan-module C/OBJ into IR products subset (A30-folded path-dep modules only emit lean -c / cc -c under dep/.slake-native/ and feed those .o into NATIVE_IRLINK / NATIVE_AR / NATIVE_EXE; order path-dep C → root C → path-dep obj → root obj → IR products — not Lake lean_lib shared facet / not freestanding build TCB / not CLAIMED / not every dep plan module) + A32 freestanding-adjacent path-dep plan-module nodes in NATIVE_GRAPH + NATIVE_SEAL subset (A30-folded modules list root-package-relative olean paths in graph and olean_hash in seal — not freestanding build TCB / not Lake build graph TCB / not Lake resolve-deps / not CLAIMED / not every dep plan module / not git/url / not multi-..) + A35 freestanding-adjacent plan-module C source inventory in NATIVE_GRAPH + NATIVE_SEAL subset (distinct `c_source <Mod> <package-cwd-relative .c path>` graph lines + seal `module <Mod> c_hash` FNV-1a 64 of `.c` bytes; soft-omit missing; banners only when ≥1 line; seal digest folds sorted c_hash after olean_hash before obj_hash — not freestanding build TCB / not Lake lean_lib facet / not CLAIMED / not every dep plan module / not changing C emit semantics / not git/url / not multi-..) + A34 freestanding-adjacent plan-module object inventory in NATIVE_GRAPH + NATIVE_SEAL subset (distinct `object <Mod> <package-cwd-relative .o path>` graph lines + seal `module <Mod> obj_hash` FNV-1a 64 of `.o` bytes; soft-omit missing; banners only when ≥1 line — not freestanding build TCB / not Lake lean_lib facet / not CLAIMED / not every dep plan module / not name-table SO / not git/url / not multi-..) + A33 path-require package `.slake-native` wipe on CLAIMED clean (after root `.lake/build` + root `.slake-native`; A26 under-pkg / A29 sibling confining; identity order; soft missing OK; symlink fence — not git/url / not Lake resolve-deps / not multi-`..` / not dep `.lake/build` / not CLAIMED token expansion) + multi-line `roots` / `defaultTargets` / `globs` double-quoted arrays (A18 — not full TOML). A7: optional sequential host-lean typecheck after plan (`SLAKE_NATIVE_CHECK=1`; not freestanding build TCB / not CLAIMED). A8: optional multi-module host-lean compile writing oleans into package-local `.slake-native/` + LEAN_PATH feed (`SLAKE_NATIVE_OLEAN=1`; default sequential; not freestanding build TCB / not lake-equivalent TCB / not CLAIMED). A9: mtime + plan-edge cascade cache invalidation subset (`SLAKE_NATIVE_OLEAN_FORCE=1` rebuilds all). A10: FNV-1a 64 source content-hash sidecar subset (touch-without-edit → skip) + `SLAKE_NATIVE_BUILD=1` freestanding-adjacent native olean build that skips lake on success. A11: plan-node transitive deps-hash (direct plan-import dep hash-fresh snapshot + frozen `deps <hex>` + skip-heal; cascade multi-hop; plan-import edges + A28 path-dep import source-hash fold — not Lake package-transitive / not freestanding build TCB / not CLAIMED). A12: optional `SLAKE_NATIVE_OLEAN_JOBS=N` ready-set parallel host-lean olean waves (default sequential; not freestanding build TCB / not Lake job server TCB / not CLAIMED / not shared-lib link). A19: optional `SLAKE_NATIVE_C=1` host lean C-output emit subset after oleans (`.slake-native/<ModRel>.c` via `lean -c`; not freestanding build TCB / not Lake lean_lib SO of compiled Lean IR / not object compile+link of Lean runtime / not CLAIMED; with NATIVE_BUILD, skip-lake requires C emit success). A20: optional `SLAKE_NATIVE_OBJ=1` host object compile of lean C after C emit (`.slake-native/<ModRel>.o` via `cc -c -fPIC -I<leanInclude>`; implies NATIVE_C; not freestanding build TCB / not Lake lean_lib SO / not linking Lean runtime into SO / not CLAIMED; with NATIVE_BUILD, skip-lake requires object compile success). A21: optional `SLAKE_NATIVE_IRLINK=1` host leanc IR shared-lib link of plan-module objects + Lean runtime via leanc (`.slake-native/libslake_ir.so` via `leanc -shared`; implies NATIVE_OBJ + NATIVE_C; not freestanding build TCB / not Lake lean_lib shared-object TCB / not CLAIMED; A13 remains separate name-table SO; with NATIVE_BUILD, skip-lake requires IR link success). A22: optional `SLAKE_NATIVE_AR=1` host static archive of plan-module objects via ar rcs (`.slake-native/libslake_ir.a` via `ar rcs`; implies NATIVE_OBJ + NATIVE_C; does not imply IRLINK; not freestanding build TCB / not Lake lean_lib static/shared facet / not CLAIMED / not linking Lean runtime into the archive; with NATIVE_BUILD, skip-lake requires archive success). A23: optional `SLAKE_NATIVE_EXE=1` host leanc executable link of plan-module objects + stub main + Lean runtime via leanc (`.slake-native/slake_ir` via `leanc -o` + generated stub main; implies NATIVE_OBJ + NATIVE_C; does not imply IRLINK or AR; not freestanding build TCB / not Lake lean_exe / not CLAIMED / not a real application entry from lakefile; with NATIVE_BUILD, skip-lake requires executable link success). A13: optional `SLAKE_NATIVE_LINK=1` host shared-lib link subset after oleans (`.slake-native/libslake_native.so` via host `cc -shared -fPIC`; exports `slake_native_plan_modules`; not Lake lean_lib shared-object / not freestanding build TCB / not Lake shared-lib TCB / not IR link of NATIVE_OBJ `.o` / not CLAIMED; with NATIVE_BUILD, skip-lake requires link success). A14: optional `SLAKE_NATIVE_GRAPH=1` host package link graph subset after oleans (`.slake-native/slake_native_graph`; plan modules with oleans + A5 import edges + optional `shared_lib` + optional `shared_lib_ir` + optional `static_lib` + optional `executable`; A32 A30-folded path-dep plan modules as root-package-relative olean paths; not freestanding build TCB / not Lake build graph TCB / not Lake lean_lib SO / not CLAIMED; with NATIVE_BUILD, skip-lake requires graph write). A15: optional `SLAKE_NATIVE_SEAL=1` host freestanding-adjacent product seal subset after oleans (`.slake-native/slake_native_seal`; module olean_hash + optional graph_hash + optional shared_lib + optional shared_lib_ir + optional static_lib + optional executable + seal digest; A32 A30-folded path-dep olean_hash from path-dep sidecar/source; not freestanding build TCB / not Lake lean_lib SO / not Lake build graph TCB / not CLAIMED; with NATIVE_BUILD, skip-lake requires seal write; order olean → path-dep C → root C → path-dep obj → root obj → IR link → static archive → executable link → name-table link → graph → seal).
* Binary: build via `tests/slake/driver` (`lake build` → `.lake/build/bin/slake`).
  Not installed with stage1 by default.

See `doc/dev/slake.md` and `src/slake/README.md`.
-/

-- Re-export driver namespaces for `import Slake`.
-- (Imports above pull CLI + Config; freestanding product stays under Systems.*)
