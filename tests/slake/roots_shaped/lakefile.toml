name = "roots_shaped"
version = "0.1.0"
# No defaultTargets — plan modules come from lean_lib roots (A16).
# No package-level srcDir — modules resolve only via per-lib srcDir.

[[lean_lib]]
name = "Lib"
srcDir = "lib"
roots = ["Alpha", "Beta"]
